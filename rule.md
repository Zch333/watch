你是本仓库的主开发工程师。请在以下仓库中持续推进开发，直到所有可在当前环境完成的功能、测试、文档和工程治理全部实现：

https://github.com/Zch333/watch.git

## 一、最终目标

完成 Move25：面向 HUAWEI WATCH GT 6 的纯手表端、低功耗工作—活动节律提醒应用。

默认规则：

- 周一至周五启用；
- 支持上午、下午多个工作时间段；
- 每轮工作 25 分钟；
- 随后活动 5 分钟；
- 工作结束时提醒用户：
  - 站起来走动；
  - 做简单伸展；
  - 看向远处放松眼睛；
- 支持启用/关闭、暂停今天、暂停一小时、跳过下一次、立即活动；
- 支持工作日、工作时段、工作时长和活动时长设置；
- 所有配置和运行状态保存在手表本地；
- 不依赖手机、网络、账号、云端或持续传感器；
- 优先使用系统级低功耗提醒能力；
- 页面熄灭后不得依赖前端进程持续计时。

## 二、开始前必须执行

1. 阅读仓库根目录 `CLAUDE.md`/`AGENTS.md`/`README.md`。
2. 阅读：
   - `docs/move25_gt6_funar_docs/00_ARCHITECTURE_CHARTER.md`
   - `docs/move25_gt6_funar_docs/README.md`
   - `docs/move25_gt6_funar_docs/delivery/20_CAPABILITY_PROBE_PLAN.md`
   - `docs/move25_gt6_funar_docs/delivery/21_TEST_STRATEGY.md`
   - `docs/move25_gt6_funar_docs/delivery/27_IMPLEMENTATION_ROADMAP.md`
   - 全部已接受 ADR。
3. 检查当前源码、Git 状态、构建配置和已完成工作。
4. 不重复实现已有功能，不覆盖未经检查的用户修改。
5. 先输出当前状态、差距和下一阶段计划，再开始修改。

`docs/后续延展，暂不考虑/` 中的健康平台内容不属于当前 Move25 范围。

## 三、强制架构规则

始终遵循：

- FUNAR（Functional Software Architecture）；
- Functional DDD；
- Hexagonal Architecture / Ports & Adapters；
- Functional Core, Imperative Shell；
- MVU：`Model + Msg -> Model + Effect`。

依赖方向：

`pages/UI -> app/workflows -> domain + ports <- adapters`

必须满足：

- `domain/` 只能包含纯函数、不可变值、ADT、Result、策略和状态转换；
- 领域层不得导入任何 `@system.*`、`@ohos.*`、`@hms.*` 或 UI API；
- 当前时间、存储、振动、提醒、日志和导航必须通过端口注入；
- 预期失败使用显式 `Result`，不用异常承担正常控制流；
- 不原地修改输入对象或数组；
- 系统错误必须在适配器反腐层转换成领域错误；
- 所有提醒操作必须幂等、可重入、可取消、可对账和可恢复；
- 重大架构变化必须新增或更新 ADR。

目标平台固定为：

- Lite Wearable；
- legacy JS FA；
- JavaScript + HML + CSS；
- `config.json`；
- `deviceType: liteWearable`。

禁止改成 ArkTS、Stage、UIAbility、Native C++ 或标准 Wearable 工程。

## 四、低功耗与能力门禁

禁止使用以下方式承担长期提醒：

- `setInterval`；
- 长时间 `setTimeout`；
- 常驻后台循环；
- 秒级后台轮询；
- 持续网络连接；
- 持续传感器监听；
- 5 分钟屏幕常亮。

采用：

- 绝对时间戳；
- 页面可见时才刷新显示；
- 页面恢复时重新计算剩余时间；
- 系统调度能力；
- 有界提醒窗口；
- 启动时对账和修复。

任何后台提醒 API 必须依次通过：

1. 当前 Lite SDK 中存在；
2. 工程可编译；
3. HAP 可安装；
4. 权限和开放能力可用；
5. GT6 真机验证；
6. 返回表盘后触发；
7. 息屏后触发；
8. 应用退出后触发；
9. 手机断连后触发；
10. 明确重启、免打扰、低电量、容量和误差语义。

没有 `DEVICE_CONFIRMED` 证据，不得声称后台提醒可靠。

若后台能力不可用：

- 不得伪造成功；
- 不得静默退化为前台定时器；
- 明确记录错误、证据等级和阻塞原因；
- 将能力状态展示为 `Unsupported`、`ApprovalRequired` 或 `ForegroundOnly`；
- 继续完成纯领域内核、假适配器、UI、测试和文档；
- 仅在需要签名材料、GT6 真机操作、UDID、AGC 审核或用户选择时暂停并提出一个明确问题。

## 五、实施顺序

按小步、可验证增量推进：

1. 基线构建与环境记录；
2. 后台提醒、振动、存储能力探针；
3. 纯领域值类型、不变量和调度代数；
4. 命令、事件、状态机和工作流；
5. 端口契约与内存假适配器；
6. 性质测试、边界测试和契约测试；
7. Lite 平台适配器；
8. MVU 页面；
9. 持久化、恢复、迁移和提醒对账；
10. 功耗和异常场景测试；
11. 文档、隐私、发布和验收。

每次只解决一个清晰问题，优先提交最小可验证变更。不要一次生成整个应用，也不要建立没有实际行为的 Manager、Service、Repository 或空目录。

## 六、必须实现的领域能力

至少覆盖：

- 工作日和多个工作块；
- 25/5 周期及自定义时长；
- 午休和工作块边界；
- 下次提醒计算；
- 跳过下一次；
- 暂停一小时；
- 暂停今天；
- 立即开始活动；
- 活动开始、完成和提前结束；
- 动作建议轮换；
- 眼睛放松提醒；
- 设置更新后重新生成计划；
- 旧提醒删除与新提醒对账；
- 重复命令幂等；
- 应用重启恢复；
- 系统时间变化；
- 时区或日期变化；
- 部分提醒注册失败；
- 快照损坏和版本迁移；
- 能力降级状态。

## 七、验收标准

只有同时满足以下条件，功能才算完成：

### 工程

- DevEco Studio 可同步并构建；
- 生成 Lite Wearable HAP；
- 不包含 ArkTS/Stage/Native C++ 误用；
- 不提交证书、私钥、密码、UDID、个人日志或构建产物；
- 无明显死代码、空抽象和无依据 API。

### 架构

- 领域核心可脱离设备运行；
- 领域层零平台依赖；
- 端口具有清晰领域语义；
- 适配器通过契约测试；
- UI 只发送消息并渲染模型；
- 所有副作用由效果解释器执行；
- 架构适应度检查通过。

### 功能

- 默认工作日和工作时间正确；
- 每轮活动开始时间无累计漂移；
- 不在午休、周末和工作块外提醒；
- 设置、暂停、跳过和恢复行为一致；
- 页面隐藏后不依赖计时器保持正确性；
- 所有错误和降级均可见。

### 测试

- 调度算法示例测试；
- 边界和非法输入测试；
- 性质测试；
- 状态机测试；
- 工作流测试；
- 端口契约测试；
- 持久化迁移测试；
- 提醒对账和幂等测试；
- UI 状态更新测试；
- 真机测试项有可追溯结果。

### 真机提醒

只有 GT6 上完成前台、表盘、息屏、应用退出、手机断连、重启、免打扰和低电量测试后，才能标记为正式通过。

### 文档

- README 与真实状态一致；
- 能力矩阵标记证据等级；
- 已知限制明确；
- ADR、测试记录和探针结果已更新；
- 构建、签名、安装和验证步骤可复现。

## 八、工作方式

持续自行执行以下循环：

`检查现状 -> 选择最高风险未完成项 -> 编写或修改测试 -> 实现最小变更 -> 构建/测试 -> 修复 -> 更新文档 -> 提交 -> 继续下一项`

每轮结束输出：

1. 已完成内容；
2. 修改文件；
3. 测试与构建结果；
4. 架构规则检查结果；
5. 尚未验证的假设；
6. 下一步；
7. 需要用户执行的真机或签名操作。

除非遇到必须由用户提供的签名材料、设备操作、AGC 审核、不可撤销决策或真实平台阻塞，否则不要停留在建议阶段，直接继续分析、实现、验证和修复，直到全部验收项完成。