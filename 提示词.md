我查了你仓库当前 `master` 的最新状态。**最可能的第一原因不是“UI 没写出来”，而是你现在只完成了 Build，没有真正执行 Run/部署到轻量级穿戴仿真器。**

你仓库最近提交里记录的命令明确是：

```text
... hvigorw.js ...
assembleHap
```

也就是**只组装 HAP**。`assembleHap` 成功只能说明“代码编译并打包成功”，不等于“已经安装并启动到仿真器”。

华为的 DevEco 流程也是把“构建”和“运行/安装”分开的：运行应用需要点击 **Run** 并选择目标设备；HAP 也可以独立安装到模拟器。([Huawei Developer][1])

## 你现在先这样操作

不要再点：

```text
Build
→ Build Hap(s)/APP(s)
→ Build Hap(s)
```

而是在 DevEco Studio 顶部：

```text
1. 启动 Huawei 轻量级穿戴设备仿真器
2. 顶部设备选择框选择该 Lite Wearable 仿真器
3. Run Configuration 选择 entry
4. product 选择 default
5. 点击绿色 ▶ Run
```

你真正需要看到的日志流程应该接近：

```text
Build HAP
→ Install / Deploy
→ Start MainAbility
→ Move25 Application onCreate
→ pages/home/index
```

而不是最终只有：

```text
assembleHap
BUILD SUCCESSFUL
```

**如果只有后者，应用根本还没有被启动。**

---

## 仓库当前入口本身已经基本正确

我重新检查了当前 `config.json`：

```json
"deviceType": [
  "liteWearable"
]
```

而且第一页已经是：

```json
"pages": [
  "pages/home/index",
  "pages/more/index",
  "pages/break-due/index",
  "pages/break-active/index",
  "pages/settings/index",
  "pages/diagnostics/index"
]
```

所以目前已经不是之前 Probe 页作为首页的问题。理论上，只要 HAP 被正确启动，首先应该进入：

```text
pages/home/index
```

首页也确实有实际 HML：

```html
<div class="container">
    <text class="banner" ...>{{capabilityText}}</text>
    <text class="status">计划：{{planStatusText}}</text>
    <text class="next">下次活动 {{nextBreak}}</text>

    <input type="button"
           class="btn-primary"
           value="立即活动"
           onclick="onStartNow">
    </input>

    ...
</div>
```

所以“GitHub 当前根本没有手表 UI”也不是原因。

---

# 如果你已经点了 Run，但仍然黑屏/空白

那问题就从“没有部署”进入第二层：

> **HAP 编译成功，但 JavaScript 在 Lite Wearable 运行时启动失败。**

这一点在你当前仓库里确实存在几个值得优先排查的风险。

## 第一嫌疑：模拟器启动代码在首页绘制前已经执行大量逻辑

你当前 `app.js` 并不是简单 Hello World，而是在 `onCreate()` 阶段立即创建：

```text
Clock
Calendar
Store
Reminder
Haptics
Diagnostics
Router
完整 Device App
MVU Shell
启动恢复
能力探测
计划对账
```

当前代码：

```js
onCreate() {
    console.info('Move25 Application onCreate');

    const now = instant(Date.now());

    const calendar = createFixedCalendar(480);

    initDeviceApp(createDeviceApp, {
        clock: createFixedClock(now.value),
        calendar: calendar,
        store: createMemoryStore(),
        reminders: createRecordingReminder({
            calendar: calendar
        }),
        haptics: createMemoryHaptics(),
        diagnostics: createMemoryDiagnostics(),
        navigation: createRouterAdapter()
    });
}
```

也就是说，**任何一个适配器或 JavaScript 内建对象在 Lite 运行时不兼容，都可能导致首页还没渲染就失败。**

---

# 当前我最想先排除的是 `Map`

你的模拟提醒适配器里面有：

```js
const registry = new Map();
```

随后大量使用：

```js
registry.has(...)
registry.set(...)
registry.get(...)
registry.delete(...)
registry.forEach(...)
```

这里有一个很关键的区别：

```text
DevEco 编译器接受 ES 代码
        ≠
Lite Wearable 运行时一定支持全部 JS 内建对象
```

目前我没有找到足够权威的 **HarmonyOS Lite 6.1.1 文档证据**，能让我直接断言 `Map` 在你这个轻量穿戴仿真器版本上“不支持”。

所以我不会说“Map 一定是原因”。

但从当前启动路径来看，它是**非常值得第一时间排除的运行时兼容点**：

```text
app.js
→ createRecordingReminder(...)
→ new Map()
→ 如果运行时不支持
→ onCreate 中断
→ 首页来不及显示
→ 构建仍然可以成功
```

这正好符合：

> **Build Success + Run 后空白**

这种现象。

---

# 还有一个风险：`@system.router`

你的应用启动时导入：

```js
import { createRouterAdapter }
    from './adapters/ui/router-adapter.js';
```

而该 Adapter 顶层又导入：

```js
import router from '@system.router';
```

当前仓库 README 自己也仍把：

```text
页面导航 @system.router
```

标记为：

```text
INFERRED
待模拟器确认
```

而不是已经通过 Lite 仿真器验证。

因此如果 Run 后立即失败，也需要看日志是否存在：

```text
@system.router
module not found
unsupported module
ReferenceError
```

不过从华为历史 Lite JS API 看，`@system.router` 本身属于标准 JS FA 路由 API，因此它的风险低于 `Map`。华为当前文档体系也仍保留兼容 JS 类 Web 开发范式和轻量穿戴开发路径。([Huawei Developer][2])

---

# 最快的诊断方法不是继续改整个项目

现在应该做一个 **UI Smoke Test**。

目标只有：

> 判断“仿真器运行链路有问题”，还是“Move25 应用启动代码有问题”。

临时把 `app.js` 简化到：

```js
export default {
    onCreate() {
        console.info(
            '[Move25] Application onCreate OK'
        );
    },

    onDestroy() {
        console.info(
            '[Move25] Application onDestroy'
        );
    }
};
```

然后临时把首页 `index.js` 改成：

```js
export default {
    data: {
        title: 'Move25',
        status: 'Lite Wearable UI OK'
    },

    onInit() {
        console.info(
            '[Move25] home onInit'
        );
    },

    onShow() {
        console.info(
            '[Move25] home onShow'
        );
    }
};
```

首页 HML：

```html
<div class="container">
    <text class="title">
        {{title}}
    </text>

    <text class="status">
        {{status}}
    </text>

    <input type="button"
           class="btn"
           value="UI 测试">
    </input>
</div>
```

CSS：

```css
.container {
    width: 466px;
    height: 466px;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    background-color: #000000;
}

.title {
    font-size: 40px;
    color: #ffffff;
}

.status {
    font-size: 22px;
    color: #ffffff;
    margin-top: 15px;
}

.btn {
    width: 220px;
    height: 55px;
    margin-top: 20px;
    color: #ffffff;
    background-color: #333333;
}
```

然后 **Run，不是 Build**。

如果这个能显示：

```text
Move25
Lite Wearable UI OK
[ UI 测试 ]
```

那么就可以确定：

```text
DevEco          ✅
Lite 仿真器      ✅
HAP 安装         ✅
FA              ✅
HML/CSS          ✅
首页配置          ✅

Move25 启动逻辑   ❌ 某处运行时异常
```

随后再逐个恢复：

```text
Clock
→ Calendar
→ Store
→ Router
→ Haptics
→ Diagnostics
→ Reminder
→ MVU Shell
```

这样可以非常快地找到第一个失败点。

---

# 日志应该怎么看

点击 **Run** 后，在 DevEco Studio 底部打开：

```text
Log
```

或者：

```text
HiLog / Run
```

第一件事搜：

```text
Move25 Application onCreate
```

因为你的当前源码必然会打印这句话。

诊断可以直接按下面判断：

| 日志情况                               | 结论                                    |
| ---------------------------------- | ------------------------------------- |
| 完全没有 `Move25 Application onCreate` | HAP 没真正启动 / Run 配置问题                  |
| 有 `onCreate`，之后 `ReferenceError`   | JS 运行时兼容问题                            |
| `Map is not defined`               | 当前 memory reminder 不能直接跑 Lite Runtime |
| `@system.router` 相关错误              | Router Adapter 问题                     |
| 有 `home onInit`，无 UI               | HML/CSS/页面渲染问题                        |
| 有 `home onShow` 且无错误               | 优先查布局、颜色、尺寸                           |
| Run 中出现 install failed             | 安装/签名/目标设备问题                          |

---

# 还有一个 UI 小问题应该顺手修

当前首页 `.container` 没设置背景色：

```css
.container {
  width: 100%;
  height: 100%;
  ...
}
```

但大量文本都是：

```css
color: #ffffff;
```

建议立即加：

```css
.container {
    width: 100%;
    height: 100%;
    flex-direction: column;
    justify-content: center;
    align-items: center;

    /* OLED 和 Lite Wearable 明确使用黑底 */
    background-color: #000000;
}
```

否则某些仿真环境如果默认背景较亮，会出现：

```text
页面其实渲染了
+
白字对白底
=
肉眼以为“什么都没有”
```

虽然这不太可能解释所有控件都消失，但排障时应该消除这个变量。

---

# 还有一个 CSS Bug

你现在首页的主按钮是：

```html
<input class="btn-primary" ...>
```

但 CSS：

```css
.btn {
    width: 240px;
    height: 48px;
    ...
}

.btn-primary {
    background-color: #0a59f7;
}
```

因此 `btn-primary` **没有获得 `.btn` 的尺寸、字体和颜色**。

以前类似：

```html
class="btn primary"
```

可以组合样式；现在为了 Lite CSS 兼容改成单 class 后，必须把公共样式复制进去。

应该写：

```css
.btn-primary {
    width: 240px;
    height: 48px;
    margin-top: 10px;
    font-size: 20px;
    background-color: #0a59f7;
    color: #ffffff;
}
```

这不是整个界面消失的根因，但确实是当前代码里的 UI 问题。

---

# 我对当前问题的概率排序

根据你说的是：

> “构建成功后，在 Huawei 轻量级穿戴设备仿真器中不显示手表界面”

以及当前 GitHub 仓库状态，我会这样排序：

```text
① 只执行了 Build / assembleHap，没有执行 Run
   概率：最高

② Run 了，但 app.js 启动阶段 JavaScript 运行时异常
   概率：高

③ recording-reminder 的 Map 等宿主 JS 能力不兼容 Lite runtime
   概率：中高，需要日志确认

④ @system.router / 其他平台模块在仿真器中失败
   概率：中

⑤ 页面实际显示但黑/白背景、样式导致看起来为空
   概率：较低

⑥ config.json 页面入口错误
   概率：低——当前入口已经正确
```

此外，仓库 README 本身目前仍明确写着 **“模拟器 UI 验证待完成”**，因此现在正处在应该做这次 G0/UI smoke test 的阶段，而不是把“能 build”当成“能运行”。

## 你现在最应该做

按这个顺序，不要同时改十个地方：

```text
1. 确认点的是绿色 Run，不是 Build Hap
2. 确认运行目标是 Huawei 轻量级穿戴仿真器
3. 查看日志有没有 "Move25 Application onCreate"
4. 如果有，找其后的第一条 JS Error
5. 如果没有，解决部署/运行配置
6. 临时运行纯静态 Hello UI
7. 静态 UI 成功后逐个恢复 Adapter
```

**如果你把点击 Run 后的完整日志中，从 `Install/Launch` 开始到第一条错误为止的约 30–80 行贴给我，我可以直接定位到当前 GitHub 仓库的具体文件和代码行，而不需要继续猜。**

[1]: https://developer.huawei.com/consumer/cn/doc/doccenter-deveco-studio/ide-emulator-install-upload?utm_source=chatgpt.com "安装应用程序包和上传文件 - 华为HarmonyOS开发者"
[2]: https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/application-package-glossary?utm_source=chatgpt.com "应用程序包术语-开发基础知识-基础入门 - 华为HarmonyOS开发者"
